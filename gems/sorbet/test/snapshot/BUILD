
# NOTE: this build file is empty currently as there are no tests defined for the
# partial and total trees. Right now it just serves as an anchor package to
# allow the individual partial and total Gemfile.lock files to be referenced
# from WORKSPACE
