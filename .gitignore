/.bazelrc
/bazel-*
/compile_commands.json
/.gdb_history
/tags

# CI puts a checkout here for testing.
/pay-server

.vscode/

/website/lib/core/metadata.js
/website/lib/core/MetadataBlog.js

/website/translated_docs
/website/build/
/website/yarn.lock
/website/node_modules
/website/i18n/*

/runtime/Gemfile.lock

# used for fuzzing
/fuzz_corpus
/fuzz_crashers

# We are bulding gems here, so don't commit these things
Gemfile.lock
*.gem

# To fake the gemfile we copy a sorbet binary here, don't commit it
/bin/sorbet-typechecker
