ClassDef{
  kind = class
  name = EmptyTree
  symbol = <C <U <root>>>
  ancestors = [ConstantLit{
      symbol = (class ::<todo sym>)
      orig = nullptr
    }]
  rhs = [
    ConstantLit{
      symbol = (static-field ::<ErrorNode>)
      orig = nullptr
    }

    ConstantLit{
      symbol = (static-field ::<ErrorNode>)
      orig = nullptr
    }

    ConstantLit{
      symbol = (static-field ::<ErrorNode>)
      orig = nullptr
    }

    ConstantLit{
      symbol = (static-field ::<ErrorNode>)
      orig = nullptr
    }

    ConstantLit{
      symbol = (static-field ::<ErrorNode>)
      orig = nullptr
    }
  ]
}
