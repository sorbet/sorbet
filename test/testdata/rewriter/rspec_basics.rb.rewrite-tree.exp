class <emptyTree><<C <root>>> < (::<todo sym>)
  class <emptyTree>::<C RSpecBasicsTest><<C <todo sym>>> < (::<todo sym>)
    class <emptyTree>::<C <describe 'traditional describe syntax'>><<C <todo sym>>> < (<self>)
      ::Sorbet::Private::Static.sig(::T::Sig::WithoutRuntime) do ||
        <self>.void()
      end

      def <it 'still works without RSpec receiver'><<todo method>>(&<blk>)
        <self>.puts("traditional test")
      end

      "still works without RSpec receiver"

      <runtime method definition of <it 'still works without RSpec receiver'>>
    end

    class <emptyTree>::<C <describe 'using context instead of describe'>><<C <todo sym>>> < (<self>)
      ::Sorbet::Private::Static.sig(::T::Sig::WithoutRuntime) do ||
        <self>.void()
      end

      def <it 'context should work like describe'><<todo method>>(&<blk>)
        <self>.puts("context test")
      end

      begin
        "context should work like describe"
        <runtime method definition of <it 'context should work like describe'>>
      end

      class <emptyTree>::<C <describe 'nested contexts'>><<C <todo sym>>> < (<self>)
        ::Sorbet::Private::Static.sig(::T::Sig::WithoutRuntime) do ||
          <self>.void()
        end

        def <it 'should handle nesting'><<todo method>>(&<blk>)
          <self>.puts("nested context test")
        end

        "should handle nesting"

        <runtime method definition of <it 'should handle nesting'>>
      end
    end

    class <emptyTree>::<C <describe 'pending tests with xit'>><<C <todo sym>>> < (<self>)
      ::Sorbet::Private::Static.sig(::T::Sig::WithoutRuntime) do ||
        <self>.void()
      end

      def <it 'regular test'><<todo method>>(&<blk>)
        <self>.puts("regular test runs")
      end

      ::Sorbet::Private::Static.sig(::T::Sig::WithoutRuntime) do ||
        <self>.void()
      end

      def <xit 'pending test with description'><<todo method>>(&<blk>)
        <self>.puts("this should be skipped")
      end

      begin
        "regular test"
        <runtime method definition of <it 'regular test'>>
      end

      begin
        "pending test with description"
        <runtime method definition of <xit 'pending test with description'>>
      end
    end

    class <emptyTree>::<C <describe 'mixed syntax'>><<C <todo sym>>> < (<self>)
      class <emptyTree>::<C <describe 'inside describe'>><<C <todo sym>>> < (<self>)
        ::Sorbet::Private::Static.sig(::T::Sig::WithoutRuntime) do ||
          <self>.void()
        end

        def <it 'regular it block'><<todo method>>(&<blk>)
          <self>.puts("mixed syntax test")
        end

        ::Sorbet::Private::Static.sig(::T::Sig::WithoutRuntime) do ||
          <self>.void()
        end

        def <xit 'pending in context'><<todo method>>(&<blk>)
          <self>.puts("pending test")
        end

        begin
          "regular it block"
          <runtime method definition of <it 'regular it block'>>
        end

        begin
          "pending in context"
          <runtime method definition of <xit 'pending in context'>>
        end
      end

      class <emptyTree>::<C <describe 'nested describe'>><<C <todo sym>>> < (<self>)
        ::Sorbet::Private::Static.sig(::T::Sig::WithoutRuntime) do ||
          <self>.void()
        end

        def <it 'should work fine'><<todo method>>(&<blk>)
          <self>.puts("nested traditional in describe")
        end

        "should work fine"

        <runtime method definition of <it 'should work fine'>>
      end
    end

    module <emptyTree>::<C Foo><<C <todo sym>>> < ()
      class <emptyTree>::<C Baz><<C <todo sym>>> < (::<todo sym>)
      end
    end

    module <emptyTree>::<C Bar><<C <todo sym>>> < ()
      class <emptyTree>::<C Baz><<C <todo sym>>> < (::<todo sym>)
      end
    end

    module <emptyTree>::<C RSpec><<C <todo sym>>> < ()
      module <emptyTree>::<C Core><<C <todo sym>>> < ()
        class <emptyTree>::<C ExampleGroup><<C <todo sym>>> < (::<todo sym>)
        end
      end
    end

    class <emptyTree>::<C <describe 'Baz'>><<C <todo sym>>> < (<self>)
      ::Sorbet::Private::Static.sig(::T::Sig::WithoutRuntime) do ||
        <self>.void()
      end

      def <it 'should work fine'><<todo method>>(&<blk>)
        <self>.puts("nested traditional in describe")
      end

      "should work fine"

      <runtime method definition of <it 'should work fine'>>
    end

    class <emptyTree>::<C <describe 'Baz'>><<C <todo sym>>> < (<emptyTree>::<C RSpec>::<C Core>::<C ExampleGroup>)
      ::Sorbet::Private::Static.sig(::T::Sig::WithoutRuntime) do ||
        <self>.void()
      end

      def <it 'should work fine'><<todo method>>(&<blk>)
        <self>.puts("nested traditional in describe")
      end

      "should work fine"

      <runtime method definition of <it 'should work fine'>>
    end
  end
end
