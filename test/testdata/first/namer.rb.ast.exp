begin
  class <emptyTree>::NormalClass<<todo sym>> < ()
    def normal_method<<todo sym>>()
      <emptyTree>

    def self.normal_static_method<<todo sym>>()
      <emptyTree>

    class <emptyTree>::InnerClass<<todo sym>> < ()
      <emptyTree>

    end

    module <emptyTree>::InnerModule<<todo sym>> < ()
      <emptyTree>

    end

  end
  module <emptyTree>::ANamespace<<todo sym>> < ()
    class <emptyTree>::ObviousChild<<todo sym>> < ()
      <emptyTree>

    end

  end
  class <emptyTree>::ANamespace::ClassInNamespace<<todo sym>> < ()
    <emptyTree>

  end
  class <emptyTree>::Parent<<todo sym>> < ()
    <emptyTree>

  end
  module <emptyTree>::Mixin<<todo sym>> < ()
    <emptyTree>

  end
  module <emptyTree>::OtherMixin<<todo sym>> < ()
    <emptyTree>

  end
  class <emptyTree>::Child<<todo sym>> < (<emptyTree>::Parent)
    self(TODO).include(<emptyTree>::Mixin)

    self(TODO).include(<emptyTree>::Mixin, <emptyTree>::Mixin)

    self(TODO).include(3)

    self(TODO).include(<emptyTree>::Mixin) do ||
      <emptyTree>
    end

    self(TODO).whatever().include(<emptyTree>::OtherMixin)

  end
end
