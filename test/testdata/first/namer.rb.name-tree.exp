begin
  class <emptyTree>::NormalClass<NormalClass> < (::Object)
    def normal_method<normal_method>()
      <emptyTree>

    <Not Supported (DefS)>

    class <emptyTree>::InnerClass<InnerClass> < (::Object)
      <emptyTree>

    end

    module <emptyTree>::InnerModule<InnerModule> < ()
      <emptyTree>

    end

  end
  module <emptyTree>::ANamespace<ANamespace> < ()
    class <emptyTree>::ObviousChild<ObviousChild> < (::Object)
      <emptyTree>

    end

  end
  class <emptyTree>::ANamespace::ClassInNamespace<ClassInNamespace> < (::Object)
    <emptyTree>

  end
  class <emptyTree>::Parent<Parent> < (::Object)
    <emptyTree>

  end
  module <emptyTree>::Mixin<Mixin> < ()
    <emptyTree>

  end
  module <emptyTree>::OtherMixin<OtherMixin> < ()
    <emptyTree>

  end
  class <emptyTree>::Child<Child> < (::Parent)
    self(<singleton class:Child>).include(::Mixin)

    self(<singleton class:Child>).include(::Mixin, ::Mixin)

    self(<singleton class:Child>).include(3)

    self(<singleton class:Child>).include(::Mixin) do ||
      <emptyTree>
    end

    self(<singleton class:Child>).whatever().include(::OtherMixin)

  end
end
