begin
  class <emptyTree>::Outer1<Outer1> < ()
    <emptyTree>

  end
  class <emptyTree>::Outer2<Outer2> < ()
    class <emptyTree>::C<C> < ()
      <emptyTree>

    end

    class <emptyTree>::Inner1<Inner1> < ()
      class <emptyTree>::A<A> < ()
        <emptyTree>

      end

      class <emptyTree>::Inner2<Inner2> < ()
        ::Outer2::Inner1::A

        ::Outer2::Inner1

        ::Outer2::Inner1::Inner2

        ::Outer1

      end

    end

    class <emptyTree>::Inner1::Inner2<Inner2> < ()
      ::Outer2::C

      ::untyped

    end

    class <emptyTree>::Other<Other> < (::Outer2::Inner1::Inner2)
      <emptyTree>

    end

  end
end
