class <emptyTree><<C <root>>> < (::<todo sym>)
  class ::Main<<C Main>> < (::<todo sym>)
    def yielder(<blk>)
      begin
        a = <implicit yield>.call(1)
        <implicit yield>.call(a)
      end
    end

    def blockpass(blk)
      begin
        a = blk.call(1)
        blk.call(a)
      end
    end

    def mixed(blk)
      begin
        a = blk.call(1)
        blk.call(a)
      end
    end

    def blockyield(<blk>)
      <self>.yielder() do |i$1|
        <implicit yield>.call(i$1)
      end
    end

    def main(<blk>)
      begin
        l = <self>.lambda() do |x$1|
          begin
            <self>.puts(x$1)
            3
          end
        end
        ::<Magic>.<call-with-block-pass>(<self>, :yielder, l)
        ::<Magic>.<call-with-block-pass>(<self>, :blockpass, l)
        ::<Magic>.<call-with-block-pass>(<self>, :mixed, l)
        ::<Magic>.<call-with-block-pass>(<self>, :blockyield, l)
      end
    end

    <runtime method definition of yielder>

    <runtime method definition of blockpass>

    <runtime method definition of mixed>

    <runtime method definition of blockyield>

    <runtime method definition of main>
  end

  ::Main.new().main()
end
