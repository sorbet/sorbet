begin
  <emptyTree>
  class <emptyTree>::<constant:Main><<constant:Main>> < (::<constant:<todo sym>>)
    <emptyTree>

    <emptyTree>

    <emptyTree>

    <emptyTree>

    <emptyTree>

    def yielder<yielder>(<blk>)
      begin
        a = <blk>.call(1)
        <blk>.call(a)
      end
    end

    def blockpass<blockpass>(blk)
      begin
        a = blk.call(1)
        blk.call(a)
      end
    end

    def mixed<mixed>(blk)
      begin
        a = blk.call(1)
        blk.call(a)
      end
    end

    def blockyield<blockyield>()
      self(<constant:Main>).yielder() do |i$5818, <blk>$5818|
        <blk>$5818.call(i$5818)
      end
    end

    def main<main>()
      begin
        l = self(<constant:Main>).lambda() do |x$5822|
          begin
            self(<constant:Main>).puts(x$5822)
            3
          end
        end
        self(<constant:Main>).yielder() do |<block-pass>$2$5824|
          l.to_proc().call(::<constant:<Magic>>.<splat>(<block-pass>$2$5824.to_a()))
        end
        self(<constant:Main>).blockpass() do |<block-pass>$3$5826|
          l.to_proc().call(::<constant:<Magic>>.<splat>(<block-pass>$3$5826.to_a()))
        end
        self(<constant:Main>).mixed() do |<block-pass>$4$5828|
          l.to_proc().call(::<constant:<Magic>>.<splat>(<block-pass>$4$5828.to_a()))
        end
        self(<constant:Main>).blockyield() do |<block-pass>$5$5830|
          l.to_proc().call(::<constant:<Magic>>.<splat>(<block-pass>$5$5830.to_a()))
        end
      end
    end
  end
  ::<constant:Main>.new().main()
end
