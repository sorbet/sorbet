begin
  class <emptyTree><<constant:<root>>> < ()
    <emptyTree>

    <emptyTree>

    def self.<static-init><<static-init>$65>()
      ::Main.new().main()
    end
  end
  class <emptyTree>::<constant:Main><<constant:Main>> < (::<todo sym>)
    <emptyTree>

    <emptyTree>

    <emptyTree>

    <emptyTree>

    <emptyTree>

    def yielder<yielder>(<blk>)
      begin
        a = <blk>.call(1)
        <blk>.call(a)
      end
    end

    def blockpass<blockpass>(blk)
      begin
        a = blk.call(1)
        blk.call(a)
      end
    end

    def mixed<mixed>(blk)
      begin
        a = blk.call(1)
        blk.call(a)
      end
    end

    def blockyield<blockyield>()
      self(<constant:Main>).yielder() do |i$5, <blk>$5|
        <blk>$5.call(i$5)
      end
    end

    def main<main>()
      begin
        l = self(<constant:Main>).lambda() do |x$7|
          begin
            self(<constant:Main>).puts(x$7)
            3
          end
        end
        self(<constant:Main>).yielder() do |<block-pass>$2$8|
          l.to_proc().call(::<Magic>.<splat>(<block-pass>$2$8.to_a()))
        end
        self(<constant:Main>).blockpass() do |<block-pass>$3$9|
          l.to_proc().call(::<Magic>.<splat>(<block-pass>$3$9.to_a()))
        end
        self(<constant:Main>).mixed() do |<block-pass>$4$10|
          l.to_proc().call(::<Magic>.<splat>(<block-pass>$4$10.to_a()))
        end
        self(<constant:Main>).blockyield() do |<block-pass>$5$11|
          l.to_proc().call(::<Magic>.<splat>(<block-pass>$5$11.to_a()))
        end
      end
    end
  end
  <emptyTree>
end
