begin
  {}
  {:a => "a"}
  self(<todo sym>).x()
  self(<todo sym>).x()
  {:a => "a"}.merge(self(<todo sym>).x())
  {:a => "a"}.merge(self(<todo sym>).x())
  self(<todo sym>).x().merge({:a => "a"})
  self(<todo sym>).x().merge({:a => "a"})
  {:a => "a"}.merge(self(<todo sym>).x()).merge(self(<todo sym>).y()).merge({:b => "b", :c => "c"}).merge(self(<todo sym>).y()).merge({:d => "d"})
  {:a => "a"}.merge(self(<todo sym>).x()).merge(self(<todo sym>).y()).merge({:b => "b", :c => "c"}).merge(self(<todo sym>).y()).merge({:d => "d"})
end
