class <emptyTree><<C <U <root>>>> < ()
  def <U main><<C <U <todo sym>>>>(&<U <blk>>)
    begin
      <U try> = 0
      if <U try>.<U <>(3)
        begin
          <U try> = <U try>.<U +>(1)
          self(<C <U <todo sym>>>).<U raise>("e")
        end
      else
        <emptyTree>
      end
    rescue => <D <U <rescueTemp>> $2>
      begin
        self(<C <U <todo sym>>>).<U puts>("rescue")
        retry
        1
      end
    end
  end

  self(<C <U <todo sym>>>).<U main>()
end
