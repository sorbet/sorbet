# -- test/testdata/infer/suggest_new.rb --
# typed: true
class ABD
end
class ABE
end

ABF = 123

ABC.new(1) # error: Method `ABC` does not exist on `T.class_of(<root>)`
ABD.new(1) # error: Method `ABD` does not exist on `T.class_of(<root>)`

class Container
  class Inner
  end

  def self.test
    Inner.new(1) # error: Method `Inner` does not exist on `T.class_of(Container)`
  end
end

class Parent
  class Inner
  end
end

class Child < Parent
  Inner.new(1) # error: Method `Inner` does not exist on `T.class_of(Child)`
end

Child::Inner.new(1) # error: Method `Inner` does not exist on `T.class_of(Child)`
# ------------------------------
