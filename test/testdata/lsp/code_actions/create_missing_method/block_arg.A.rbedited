# typed: true
# selective-apply-code-action: refactor
# enable-experimental-lsp-create-missing-method: true

class BlockArg
  extend T::Sig

  sig { void }
  def caller
    with_block(1) do |x|
#   ^^^^^^^^^^ error: Method `with_block` does not exist on `BlockArg`
#     ^ apply-code-action: [A] Create missing method
      puts x
    end
  end

  sig { params(param: Integer, blk: T.untyped).returns(T.untyped) }
  def with_block(param, &blk)
    Kernel.raise NotImplementedError
  end
end
