# typed: strict
# selective-apply-code-action: refactor.extract

module IncModule
  extend T::Sig
  sig {params(x: Integer).returns(Integer)}
  def inc(x)
    # ^ apply-code-action: [A] Move method to a new module
    x + 1
  end
end

class Inc
  extend T::Sig

end

foo = Inc.new
res = IncModule.inc(42)

other_res = IncModule.inc(42)
