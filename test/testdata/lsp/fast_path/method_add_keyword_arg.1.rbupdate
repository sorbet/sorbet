# typed: true
# assert-fast-path: method_add_keyword_arg.rb

class A extend T::Sig
  sig {params(x: Integer, y: Integer).returns(String)}
  def bar(x:, y:)
    res = x.to_s
    T.reveal_type(y) # error: `Integer`
    res
  end
end

A.new.bar
#        ^ error: Missing 2 required keyword arguments for method `A#bar`
A.new.bar(x: 0) # error: Missing required keyword argument `y` for method `A#bar`
A.new.bar(x: 0, y: 0)
