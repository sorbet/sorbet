# typed: true
# frozen_string_literal: true

require_relative './constant__class_definition.rb'

sig { params(foo: Bar::Foo).returns(Bar::Foo) }
def foo(foo); end

class Baz
#     ^ apply-rename: [D] newName: Bar invalid: true

end
