# typed: true
# frozen_string_literal: true

module M
  class Foo
    def baz(a)
#     ^ apply-rename: [A] newName: baz
    end

    def caller
      baz(1)
    end
  end
end

f = M::Foo.new
f.baz(2)

M::Foo.new.baz 3

class Unrelated
  # this should be left unchanged
  def bar; end
end
