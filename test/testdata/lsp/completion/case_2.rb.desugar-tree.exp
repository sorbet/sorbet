class <emptyTree><<C <root>>> < (::<todo sym>)
  module <emptyTree>::<C Opus>::<C Log><<C <todo sym>>> < ()
    def self.info<<todo method>>(msg, &<blk>)
      <emptyTree>
    end
  end

  class <emptyTree>::<C ConstantCompletion><<C <todo sym>>> < (::<todo sym>)
    <emptyTree>::<C A> = nil

    <emptyTree>::<C B> = nil

    def self.test1<<todo method>>(x, &<blk>)
      begin
        <self>.puts("before")
        if <emptyTree>::<C ConstantCompletion>::<C <ConstantNameMissing>>.===(x)
          <self>.puts("hello")
        else
          <emptyTree>
        end
      end
    end

    def self.test2<<todo method>>(x, &<blk>)
      begin
        <self>.puts("before")
        if <emptyTree>::<C ConstantCompletion>.puts("hello").===(x)
          <emptyTree>
        else
          <emptyTree>
        end
      end
    end

    def self.test3<<todo method>>(x, &<blk>)
      begin
        <self>.puts("before")
        if <emptyTree>::<C ConstantCompletion>::<C Opus>::<C Log>.info("hello").===(x)
          <emptyTree>
        else
          <emptyTree>
        end
      end
    end

    def self.test4<<todo method>>(x, &<blk>)
      begin
        <self>.puts("before")
        if <emptyTree>::<C ConstantCompletion>.y=(nil).===(x)
          <emptyTree>
        else
          <emptyTree>
        end
      end
    end
  end
end
